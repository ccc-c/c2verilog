#include <stdio.h>

int main() {
  int f1=1, f2=0;
  int f, n=8, i=2;
  while (i<=n) { // if (i>n) goto WEND
    f = f1 + f2;
    f2 = f1;
    f1 = f;
    i = i + 1;
    printf("f=%d\n", f);
  }
// WEND
}